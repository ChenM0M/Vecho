name: release

on:
  push:
    tags:
      - 'v*'

env:
  GH_REPO: ${{ github.repository }}

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Create GitHub release (generate notes)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF_NAME}"
          if gh release view "$tag" >/dev/null 2>&1; then
            echo "Release already exists: $tag"
          else
            gh release create "$tag" --generate-notes --title "$tag"
          fi

  build:
    needs: create-release
    name: build & upload (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            src-tauri -> target

      - name: Install Linux dependencies (Tauri)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install dependencies
        run: npm ci

      - name: Build (Tauri)
        run: npm run tauri:build

      - name: Prepare portable (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item "src-tauri\target\release\vecho-studio.exe" "Vecho-Portable-Windows.exe"

      - name: Prepare portable (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          cp src-tauri/target/release/vecho-studio Vecho-Portable-macOS
          chmod +x Vecho-Portable-macOS
          zip -9 Vecho-Portable-macOS.zip Vecho-Portable-macOS

      - name: Prepare portable (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          cp src-tauri/target/release/vecho-studio Vecho-Portable-Linux
          chmod +x Vecho-Portable-Linux
          tar -czvf Vecho-Portable-Linux.tar.gz Vecho-Portable-Linux

      - name: Upload bundles to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME}"
          shopt -s nullglob

          if [[ "${RUNNER_OS}" == "Windows" ]]; then
            files=(
              src-tauri/target/release/bundle/nsis/*.exe
              src-tauri/target/release/bundle/msi/*.msi
              Vecho-Portable-Windows.exe
            )
          elif [[ "${RUNNER_OS}" == "macOS" ]]; then
            files=(
              src-tauri/target/release/bundle/dmg/*.dmg
              src-tauri/target/release/bundle/macos/*.app.tar.gz
              Vecho-Portable-macOS.zip
            )
          else
            files=(
              src-tauri/target/release/bundle/appimage/*.AppImage
              src-tauri/target/release/bundle/deb/*.deb
              src-tauri/target/release/bundle/rpm/*.rpm
              Vecho-Portable-Linux.tar.gz
            )
          fi

          if (( ${#files[@]} == 0 )); then
            echo "No bundle files found."
            exit 1
          fi

          gh release upload "$tag" "${files[@]}" --clobber
